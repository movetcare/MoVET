// import {functions} from './config';
// const client = require('firebase-tools');

export const deleteConfig = async () => {
  // const collections = ['configuration', 'alerts', 'events'];
  // return await Promise.all(
  //   collections.map(async (collection: string) => {
  //     await client.firestore.delete(collection, {
  //       project: functions.config()?.environment.project_id,
  //       recursive: true,
  //       yes: true,
  //     });
  //   })
  // ).then(async () => true);
};
