name: PRODUCTION - Deploy Storage Security Rules
on:
  push:
    branches:
      - main
    paths:
      - "apps/api/storage.rules"
jobs:
  deploy:
    name: Deploy Storage Security Rules
    runs-on: ubuntu-latest
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL_PRODUCTION }}
    steps:
      - name: Checkout Repo
        id: repo-checkout
        uses: actions/checkout@v2.3.2
      - name: Deploy to Firebase
        id: deploy-rules
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only storage:rules --non-interactive -f
        env:
          PROJECT_ID: movet-care
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN_PRODUCTION }}
      - uses: act10ns/slack@v1
        with:
          status: ${{ job.status }}
          steps: ${{ toJson(steps) }}
          channel: "#production-logs"
        if: always()
