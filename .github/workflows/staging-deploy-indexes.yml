name: STAGING - Deploy Firestore Indexes
on:
  push:
    branches:
      - development
    paths:
      - "firestore.indexes.json"
jobs:
  deploy:
    name: Deploy Indexes
    runs-on: ubuntu-latest
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_STAGING_LOGS }}
    steps:
      - name: Checkout Repo
        id: repo-checkout
        uses: actions/checkout@v2.3.2
      - name: Deploy to Firebase
        id: deploy-indexes
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only firestore:indexes --non-interactive -f
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN_STAGING }}
      - uses: act10ns/slack@v1
        with:
          status: ${{ job.status }}
          steps: ${{ toJson(steps) }}
          channel: "#development-feed"
        if: always()
