import { AppointmentDetail as AptDetail } from "components/AppointmentDetail";
import { Screen } from "components/themed";

const AppointmentDetail = () => {
  return (
    <Screen>
      <AptDetail />
    </Screen>
  );
};

export default AppointmentDetail;
